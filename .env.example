# MongoDB connection string for the MERN data layer.
MONGODB_URI="mongodb://127.0.0.1:27017/aura_goods"

# Secret used to sign local authentication tokens.
JWT_SECRET="replace-with-a-long-random-secret"

# Payments are recorded for manual processing. No card number, expiry, or CVV is stored.
